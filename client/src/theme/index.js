import { createTheme } from "@mui/material";

export default createTheme({
    palette:{
        primary:{
            main: "rgba(100, 47, 126, 0.5735)"
        },
        primary2:{
            main: "rgba(200, 0, 0, 0.5735)"
        },
        secondary:{
            main:"rgba(207, 228, 252, 1)"
        }
    }
});